{
  "name": "team-task-manager",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install-all": "npm install --prefix server && npm install --prefix client",
    "start": "npm run dev --prefix server & npm start --prefix client",
    "dev": "npm run start"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
